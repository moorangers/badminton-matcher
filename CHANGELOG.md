# Changelog

## v0.9.25 (2026-09-07)

- **กดพื้นที่ว่างด้านนอก modal ตอนนี้ปิด modal ได้แล้ว** (ก่อนหน้านี้ต้องกดปุ่มยกเลิก/X เท่านั้น) — ใช้ได้กับ modal ทุกอันในแอป (สร้าง session, ปลดล็อก session, จัดการผู้เล่น, ยืนยันต่างๆ, เปลี่ยน PIN, ลิงก์ส่วนตัว) ยกเว้นหน้ากรอก PIN หลักที่ไม่มีปุ่มยกเลิกอยู่แล้ว (ต้องกรอก PIN หรือกด "เริ่ม session ใหม่" เท่านั้น)
- **เพิ่มเมนูแฮมเบอร์เกอร์สำหรับมือถือ** — ปุ่มลิงก์ในเฮดเดอร์ (กระดานข่าว/วิธีใช้งาน/จอคะแนนสด/ออกจาก session) ที่เคยเบียดกันเป็นไอคอนล้วนบนจอเล็ก ยุบรวมเป็นเมนูเดียวเปิด/ปิดด้วยไอคอนแฮมเบอร์เกอร์แทน (จอใหญ่ยังเป็นแถวปุ่มแบบเดิม ไม่กระทบ) กดพื้นที่ว่างด้านนอกก็ปิดเมนูได้เหมือน modal อื่นๆ

## v0.9.24 (2026-09-07)

- **กดการ์ด session ในลิสต์ "Session ล่าสุด" เปลี่ยนเป็นเปิด modal ใส่ PIN แทนการสลับไปหน้าใหม่** — ลิสต์ session ยังอยู่มองเห็นได้เต็มด้านหลัง modal ตลอด (กรอก PIN ผิดก็ยังอยู่ที่เดิม กดปิด (X) ก็กลับมาที่ลิสต์ได้เลยไม่ต้องรีเฟรช) ก่อนหน้านี้กดแล้วเปลี่ยนไปเป็นหน้าเต็มจอทันทีซึ่งรู้สึกเหมือนเปลี่ยนหน้าไปคนละที่

## v0.9.23 (2026-09-07)

- **หน้ากรอก PIN เปลี่ยนเป็นสไตล์ modal** (การ์ดลอย backdrop-blur เหมือน modal อื่นๆ ในแอป แทนการ์ดเต็มหน้าจอแบบเดิม)
- **ช่อง PIN ทุกจุดพิมพ์แล้วขึ้น •••• แทนตัวเลขจริง** (ใส่ตอนสร้าง session, หน้ากรอก PIN, เปลี่ยน PIN ทั้งช่องเดิม/ใหม่, ลิงก์ส่วนตัว) กันคนแอบมองเห็น PIN ตอนพิมพ์

## v0.9.22 (2026-09-07)

- **เพิ่ม "ลิงก์เข้าใช้งานส่วนตัว" — เปิดแล้วเข้า dashboard ได้เลยไม่ต้องพิมพ์ PIN** ดู [decision-log.md#adr-020](./docs/decision-log.md)
  - สร้าง session เสร็จ ระบบโชว์ลิงก์นี้ให้ copy/แชร์ทันที (มี PIN ฝังอยู่แล้ว ไม่ต้องพิมพ์ซ้ำ) — เก็บไว้เอง (โน้ต/แชทตัวเอง) กันลืม PIN ในอนาคต
  - หรือกดปุ่ม "ลิงก์ส่วนตัว" ในการ์ดลิงก์เช็คอินได้ทุกเมื่อ (ต้องยืนยัน PIN ปัจจุบันก่อนถึงจะสร้างลิงก์ให้)
  - ลิงก์นี้แยกจากลิงก์เช็คอิน/QR ที่แชร์ให้ผู้เล่น (อันนั้นยังไม่ต้องใช้ PIN เหมือนเดิม) — ห้ามแชร์ลิงก์ส่วนตัวให้ผู้เล่น เพราะมีค่าเหมือนรู้ PIN ของ session นั้น

## v0.9.21 (2026-09-07)

- **เพิ่มปุ่ม "เปลี่ยน PIN"** ในการ์ดลิงก์เช็คอิน — กรอก PIN ปัจจุบัน + PIN ใหม่ (4-6 หลัก) ตรวจ PIN ปัจจุบันถูกต้องฝั่ง server ก่อนเปลี่ยน ข้อมูล session อื่นไม่กระทบ ดู [decision-log.md#adr-019](./docs/decision-log.md) — ยังไม่ใช่ "ลืม PIN" เต็มรูปแบบ (ต้อง login ค้างอยู่ในเครื่องนั้นก่อนถึงจะเปลี่ยนได้)
- **เพิ่ม loading spinner ให้ปุ่มที่ยิง API ทั่วแอป** เพราะตอนนี้ต่อ MongoDB จริงแล้ว (ไม่ใช่ localStorage) latency จาก network อาจทำให้ปุ่มดูเหมือนไม่ตอบสนอง — ครอบคลุมปุ่ม: เพิ่ม/ลบ/เช็คอินผู้เล่น, บันทึกชื่อผู้เล่น/สลับผู้เล่น, บันทึกแผนรอบถัดไป, เริ่มจับคู่, รีเซ็ตสถิติ/แมตช์, เริ่ม/จบแมตช์/ปิดคอร์ด/ย้อนกลับต่อคอร์ด (ล็อกเฉพาะคอร์ดที่กำลังทำงานอยู่ คอร์ดอื่นกดได้ปกติ)

## v0.9.20 (2026-09-06)

- **หน้าแรกจัด layout ใหม่ทั้งหมด — เหลือการ์ด "Session ล่าสุด" เป็น content หลัก, ย้ายฟอร์มสร้าง session ไปเป็น modal** — ดู [decision-log.md#adr-018](./docs/decision-log.md)
  - ปุ่ม "สร้าง session" ย้ายไปอยู่ในเฮดเดอร์ (คู่กับกระดานข่าว/วิธีใช้งาน) กดแล้วเปิด modal มีแค่ตั้งชื่อ (ไม่บังคับ) + PIN + ปุ่มสร้าง เหมือนเดิม (โหมด/คอร์ด/คะแนนยังไปปรับที่ dashboard หลังสร้างเหมือนเดิม)
  - การ์ด "Session ล่าสุด" แสดงเป็น grid กล่องสี่เหลี่ยมจัตุรัส — 2 คอลัมน์บนมือถือ, 4 คอลัมน์บน desktop ทีละ 8 รายการ (2 แถว) มี pagination ก่อนหน้า/ถัดไปถ้าเกิน ยังกรองเฉพาะที่สร้างภายใน 24 ชม. ที่ผ่านมาเหมือนเดิม
- แก้ E2E test helper เพิ่ม เพราะปุ่ม submit ใน modal ชื่อชนกับปุ่ม trigger ในเฮดเดอร์ (ข้อความเดียวกัน) เพิ่ม `data-testid="submit-create-session"` ให้กดแบบไม่กำกวม

## v0.9.18 (2026-09-06)

- **ตั้งชื่อ session ได้ (ไม่บังคับ) + เลือกเข้า session เดิมจากหน้าแรกได้** — ดู [decision-log.md#adr-017](./docs/decision-log.md)
  - หน้าสร้าง session เพิ่มช่อง "ตั้งชื่อ session" (ไม่บังคับ) และส่วน "หรือเข้า session ที่มีอยู่แล้ว" แสดง session ล่าสุด 20 อัน (ชื่อ/วันที่/โหมด/คอร์ด) ต่อกับ `GET /api/sessions` ที่มีอยู่แล้วแต่ไม่เคยถูกใช้จริง กดเลือกแล้วพาไปหน้า PIN gate ของ session นั้นได้เลย
  - โชว์ชื่อ+วันที่ของ session ปัจจุบันในหัว dashboard และหน้า PIN gate ด้วย ไม่สับสนว่ากำลังเข้า session ไหนอยู่
- แก้ E2E test helper ให้ใช้ `data-testid` แทน accessible-name สำหรับปุ่มเลือกโหมด เพราะ list "session ที่มีอยู่แล้ว" ที่โตขึ้นเรื่อย ๆ ทำให้ชื่อ session เก่า ๆ ชนกับข้อความปุ่ม "Singles"/"Doubles"

## v0.9.17 (2026-09-06)

- **แก้บั๊ก: "รีเซ็ตทั้งหมด" เตะผู้เล่นออกจากพูลจับคู่ทั้งวง** — เดิมรีเซ็ตสถานะทุกคนกลับเป็น "registered" (ต้องเช็คอินใหม่หมด) ทั้งที่ตั้งใจแค่ล้างแมตช์/สถิติ แก้แล้วให้แตะแค่คนที่กำลังเล่นอยู่ (`playing` → `checked_in` เพราะแมตช์ถูกลบ) คนอื่นไม่ถูกแตะสถานะเลย ดู [decision-log.md#adr-016](./docs/decision-log.md)
- **ย้ายปุ่ม "รีเซ็ตทั้งหมด"/"ล้างทั้งหมด"** จากเดิมที่ซ่อนอยู่ในแถบสถิติใต้ "แมตช์ปัจจุบัน" (เห็นได้เฉพาะตอนมีแมตช์ active เท่านั้น) — "ล้างทั้งหมด" (ออกจาก session) ย้ายไปเป็นปุ่มในเฮดเดอร์ (คู่กับจอคะแนนสด/กระดานข่าว/วิธีใช้งาน) แสดงตลอดเวลาตั้งแต่สร้าง session เสร็จ; ส่วน "รีเซ็ตทั้งหมด" เปลี่ยนชื่อเป็น **"รีเซ็ตสถิติ/แมตช์"** (สื่อความหมายชัดขึ้นว่าเคลียร์แค่แมตช์/สถิติ ไม่ได้เคลียร์คน) แล้วย้ายไปอยู่หัว "แมตช์ปัจจุบัน" คู่กับปุ่ม "ปรับรอบถัดไป" แทน (โชว์เฉพาะตอนมีแมตช์ active เหมือนปุ่มข้าง ๆ กัน — เป็นการตัดสินใจตั้งใจ)
- แถบสถิติ (กำลังเล่น/พักอยู่/จบแล้ว) เอา scroll แนวนอนที่เคยต้องใช้ (`overflow-x-auto`) ออก เพราะพอไม่มีปุ่มสองอันแล้วเหลือแค่ตัวเลขสั้น ๆ พอดีแถวเดียว
- ปุ่มปรับคะแนน `[+][−] [คะแนน] vs [คะแนน] [−][+]` รวมกลับมาอยู่บรรทัดเดียวกับตัวเลขคะแนนได้แล้ว (ยุบแถวปุ่มแยกทิ้ง) เพราะตอนนี้แถวคะแนนไม่ต้องแชร์พื้นที่กับชื่อผู้เล่นแล้ว (ชื่อมีแถวของตัวเองแยกจากรอบก่อน) ทดสอบที่ 360px ไม่ล้น

## v0.9.16 (2026-09-06)

- **เพิ่ม automated test 2 ระดับแรกของโปรเจกต์** (เดิมไม่มี test/CI เลย) — ดู [decision-log.md#adr-015](./docs/decision-log.md) และ README ส่วน "Testing":
  - **Vitest unit test** (`yarn test`) — แยก matching algorithm ออกจาก `home-page.tsx` ไปเป็น `src/lib/matching.ts` เพื่อให้เทสได้ เขียนเทส 21 เคสครอบคลุม `buildMatchesFromPlayers`/`assignTeams`/`rankCandidates` และ `getGameWinner`
  - **Playwright E2E test** (`yarn test:e2e`) — เก็บ regression test ให้ 2 บัคที่เพิ่งเจอและแก้ไปวันนี้: `tests/e2e/undo-finish.spec.ts` (v0.9.14) และ `tests/e2e/add-court.spec.ts` (v0.9.15)

## v0.9.15 (2026-09-06)

- **แก้บั๊ก: เพิ่มคอร์ดใหม่ระหว่างเล่นไม่ได้จริง** — กด "ปรับรอบถัดไป" เพิ่มคอร์ดใหม่ (เช่น Singles คอร์ดเดียว เพิ่มเป็น 2 คอร์ด) เดิมแค่อัปเดตค่า `activeCourts` แต่ไม่มีจุดไหนสร้างแมตช์จริงให้คอร์ดใหม่เลย (แมตช์ใหม่ปกติถูกสร้างตอน "จบแมตช์" ของคอร์ดเดิมเท่านั้น ซึ่งคอร์ดที่เพิ่งเพิ่มไม่เคยมีมาก่อนเลยไม่มีอะไร trigger) ทำให้คอร์ดใหม่ค้างเป็นแค่ "คู่ถัดไป" preview ตลอดไป กดเริ่ม/จบแมตช์ไม่ได้ แก้โดยให้ `saveNextPlan` จับคู่+สร้างแมตช์จริงทันทีให้ทุกคอร์ดที่เพิ่งเพิ่มเข้ามาและยังไม่มีแมตช์ (ทดสอบจริง: Singles 1 คอร์ด 6 คน → เพิ่ม Court 2 → บันทึกแผน → Court 2 ขึ้นแมตช์จริงทันที เริ่ม/จบแมตช์ได้ปกติ)

## v0.9.14 (2026-09-06)

- **แก้บั๊ก: "ย้อนกลับคอร์ดนี้" ใช้งานจริงไม่ได้เลย** — `finishMatch()` auto-fill แมตช์ใหม่ลงคอร์ดทันทีหลังจบแมตช์ แต่ `undo-finish` เดิมบล็อก undo ทุกครั้งที่คอร์ดมีแมตช์ใหม่อยู่แล้ว (ซึ่งเกิดขึ้นเสมอเพราะ auto-fill) ทำให้ undo ใช้ไม่ได้เลยในเคสปกติ แก้โดยอนุญาตให้ undo ยกเลิกแมตช์ auto-fill ทิ้งได้ถ้ายังเป็นสถานะ "พร้อมเริ่ม" (ยังไม่มีใครแตะ ไม่มีอะไรเสีย) แล้วคืนผู้เล่นไปพัก ก่อนดึงแมตช์เดิมกลับมา — ถ้าแมตช์ auto-fill ถูกกด "เริ่ม" ไปแล้วยังคงบล็อกเหมือนเดิมเพื่อความปลอดภัย (ทดสอบจริง 8 คน 1 คอร์ด: จบแมตช์แล้ว auto-fill คู่ใหม่ กด undo กลับไปเป็นคู่เดิมถูกต้อง)

## v0.9.13 (2026-09-06)

- **ปรับ layout การ์ดแมตช์ในหน้า "แมตช์ปัจจุบัน" ให้กดคะแนนง่ายขึ้นบนมือถือ + อ่านชื่อผู้เล่นออกเต็ม ๆ** (แทนที่ปุ่ม +/- เดิมขนาด 20×20px ที่กดยาก): จัดใหม่เป็น 4 แถวต่อการ์ด — (1) หัวการ์ด "Court X · โหมด · แต้มเป้าหมาย" คู่กับสถานะ + ปุ่ม "ปิดคอร์ด" (ย้ายมาจากแถวปุ่มด้านล่าง), (2) label TEAM A/B, (3) คะแนนปัจจุบันกล่องเหลี่ยมตัวใหญ่ `[คะแนน] vs [คะแนน]` (icon vs เป็นสีแดงตอนกำลังเล่น) กึ่งกลางการ์ด, (4) ชื่อผู้เล่น 2 คอลัมน์เต็มความกว้าง (ไม่ตัดคำแล้ว), แล้วปิดท้ายด้วยแถวปุ่ม +/- ขนาด 44px ชิดขอบซ้าย/ขวา (Team A ซ้าย, Team B ขวา) — ทดสอบจริงที่ 360-375px ตลอดหลายรอบด้วย Playwright ว่าไม่ล้นจอและกดคะแนนทำงานถูกต้อง
- ปุ่มเปลี่ยนตัวผู้เล่นขยายจาก 20px เป็น 36px, ปุ่มเริ่ม/จบแมตช์/ย้อนกลับคอร์ดขยายจาก 32px เป็น 40px ให้กดง่ายขึ้นด้วยเช่นกัน
- แก้บั๊ก CSS Grid ที่เจอระหว่างทดสอบ: `grid-cols-[1fr_auto_1fr]` ไม่ยอมให้คอลัมน์ข้างหดตัวต่ำกว่าขนาดเนื้อหา ทำให้ล้นจอแทนที่จะตัดคำ ต้องใช้ `minmax(0,1fr)` แทน

## v0.9.6 (2026-09-06)

- เพิ่ม modal ยืนยันก่อนลบโพสต์ในหน้า `/board` (กด "ลบโพสต์" ไม่ลบทันทีอีกต่อไป ต้องกดยืนยันในกล่องซ้อนก่อน) กันลบมือลั่น

## v0.9.5 (2026-09-06)

- **แก้บั๊กช่องกำหนดเองในคะแนนเป้าหมาย**: ก่อนหน้านี้ลบเลขในช่อง (เช่น ลบ "11" ทั้งหมดเพื่อพิมพ์ "7" ใหม่) ไม่ได้จริง เพราะ input ผูกกับ `value` ที่ commit แล้วโดยตรง พอช่องว่างชั่วคราวค่าจะ invalid แล้ว React ดันค่าเดิมกลับเข้าไปทันทีจนพิมพ์ต่อไม่ได้ (reproduce แล้วด้วย Playwright) — แก้โดยแยก state ของช่อง input ออกจาก `value` ที่ commit แล้ว ให้พิมพ์/ลบได้อิสระ แล้วค่อย sync กลับตอน blur ถ้าปล่อยว่างไว้

## v0.9.4 (2026-09-06)

- **ย้ายกระดานข่าวออกจากหน้า dashboard ไปเป็นเมนูบนสุด** (เหมือน "จอคะแนนสด"/"วิธีใช้งาน") — เอาการ์ดโพสต์+preview ออกจาก dashboard ทั้งหมด แล้วย้ายฟอร์มโพสต์ (แนบรูป/โพสต์) และปุ่มลบโพสต์เข้าไปอยู่ในหน้า `/board` โดยตรงแทน ลดการ์ดที่ไม่เกี่ยวกับ flow จับคู่หลักออกจากหน้าแรก
- เพิ่มปุ่ม **"แชร์"** ข้าง ๆ ลิงก์เช็คอิน/ลงชื่อ QR — ใช้ Web Share API ของเบราว์เซอร์ (เช่น แชร์เข้า LINE ได้ตรง ๆ) ซ่อนอัตโนมัติถ้าเบราว์เซอร์ไม่รองรับ (เช่น เดสก์ท็อป)
- ปรับ layout คะแนนเป้าหมายต่อเกม ให้ preset (11/15/21) กับช่องกำหนดเองอยู่บรรทัดเดียวกัน แนวเดียวกันแล้ว (เดิม label "กำหนดเอง" อยู่คนละบรรทัดทำให้เยื้องกัน)

## v0.9.3 (2026-09-06)

- เปลี่ยนค่า default คะแนนเป้าหมายต่อเกมจาก 21 เป็น **11** ทั้งฝั่ง client และ schema default (ดูอัปเดตใน [decision-log.md#adr-014](./docs/decision-log.md))
- เพิ่ม label เล็ก ๆ "กำหนดเอง" กำกับช่องกรอกตัวเลขในการ์ดคะแนนเป้าหมาย ให้เห็นชัดว่าพิมพ์เองได้ ไม่ได้จำกัดแค่ preset

## v0.9.2 (2026-09-06)

- **ปรับ UX หน้า dashboard** — ตามรีวิว UI/UX:
  - จัดลำดับการ์ดใหม่เป็น: QR เช็คอิน → กระดานข่าว → เลือกโหมด/คอร์ด/คะแนน → ผู้เล่น → ปุ่ม "เริ่มจับคู่" (เดิมผู้เล่นอยู่บนสุด สลับกับ QR/กระดานข่าวที่อยู่ท้าย)
  - เลิกโชว์การ์ดเลือกโหมด/คอร์ด/คะแนนเป้าหมาย (แบบ disabled) ซ้ำกับกล่องสรุประหว่างที่มีแมตช์กำลังเล่นอยู่ — เพราะข้อมูลเดียวกันโชว์อยู่แล้วบนหัวการ์ดแต่ละคอร์ดใน "แมตช์ปัจจุบัน" และมีปุ่ม "ปรับรอบถัดไป" ให้แก้ตรงนั้นอยู่แล้ว

## v0.9.1 (2026-09-06)

- **คะแนนเป้าหมายต่อเกมปรับได้** — ไม่ fix ที่ 21 แต้มแล้ว เลือก 11/15/21 หรือกำหนดเองได้ (1-99) ตั้งตอนสร้าง session, หน้าก่อนเริ่มจับคู่, หรือแก้ผ่าน "ปรับรอบถัดไป"
- แต่ละแมตช์เก็บ `targetScore` เป็น snapshot ของตัวเอง (เหมือน `mode`) — แก้ค่า default ของ session ระหว่างเล่นจะไม่กระทบแมตช์ที่กำลังเล่นอยู่
- cap การชนะขาด (ไม่ต้องนำห่าง 2 แต้ม) ขยายจาก "30 แต้มตายตัว" เป็น `targetScore + 9` ทั่วไป
- แสดงคะแนนเป้าหมายบนการ์ดแมตช์แต่ละคอร์ด (เช่น "· 15 แต้ม")

## v0.9.0 (2026-09-06)

- **ระบบนับคะแนน + Live Scoreboard** — ปิด Phase 4
- ปุ่ม +/- คะแนนต่อทีมบนการ์ดแมตช์แต่ละคอร์ด นับเกมเดียวตามกติกาแบดมินตัน (21 แต้ม win-by-2, cap 30) คำนวณผู้ชนะเกมอัตโนมัติ
- หน้าจอคะแนนสดสาธารณะที่ `/scoreboard/[sessionId]` ไม่ต้องมี PIN อัปเดตทุก 2 วินาที (polling) ตัวอักษรใหญ่เหมาะเปิดจอทีวี/โปรเจกเตอร์
- Endpoint ใหม่: `POST /api/sessions/:id/matches/:matchId/score`
- **แก้บั๊กที่เจอระหว่างทดสอบ**: การนับคะแนนแบบเดิม (read-modify-write) มี race condition เมื่อกดปุ่มรัว ๆ ทำให้แต้มหายบางส่วน แก้เป็น atomic `$inc` แทน
- เพิ่ม `serializeMatch`/`getGameWinner` กลางไว้ที่ `matchLifecycle.ts` ให้ทุก match endpoint ส่งค่าคะแนนออกมาสอดคล้องกัน

## v0.8.0 (2026-09-06)

- **กระดานข่าว (Webboard)** — โพสต์ข้อความ+รูปภาพจากหน้า dashboard, feed สาธารณะที่ `/board` ไม่ต้องมี PIN
- รูปอัปโหลดตรงจากเบราว์เซอร์ไปที่ **Vercel Blob** (เลี่ยง serverless request body size limit ที่รูปมือถือมักเกิน) — `POST /api/posts/upload` ทำหน้าที่ออก client token ตามรูปแบบที่ Vercel แนะนำ
- Endpoint ใหม่: `GET/POST /api/posts` (list แบบ cursor pagination + สร้างโพสต์), `DELETE /api/posts/:postId`
- ปิด Phase 1 อย่างเป็นทางการ: ตัดสินใจไม่ทำ auto-trigger ปิดคอร์ดตามเวลาจองจริง ใช้ manual ล้วนต่อไป
- **แก้ปัญหาสำคัญ**: local `.env` เผลอชี้ไปที่ MongoDB Atlas จริง (ค้างจากตอน debug deploy) ทำให้การทดสอบไปกระทบ database จริงที่ deploy ใช้งานอยู่ — ล้าง Atlas ให้สะอาดแล้ว (ยืนยันจากผู้ใช้ก่อนลบ) และย้าย local `.env` กลับไปใช้ container mongo ในเครื่องแทน เพิ่มคำเตือนไว้ใน README/decision-log กันเกิดซ้ำ

## v0.7.0 (2026-09-06)

- **เช็คอิน 2 ขั้น + ลงชื่อ/เช็คอินด้วยตัวเองผ่าน QR** — เพิ่มสถานะผู้เล่น `registered` (ลงชื่อล่วงหน้า) แยกจาก `checked_in` (ยืนยันถึงคอร์ดแล้ว) คนที่ยังไม่เช็คอินจะไม่ถูกเอาไปสุ่ม (บังคับทั้งฝั่ง client และ server)
- เพิ่มหน้า public `/checkin/[sessionId]` (ไม่ต้องมี PIN) ให้ผู้เล่นลงชื่อล่วงหน้าหรือเช็คอินเองได้ รองรับทั้งคนที่เคยลงชื่อไว้และ walk-in ที่ไม่เคยลงทะเบียนมาก่อน
- เพิ่ม endpoint สาธารณะ `POST /api/sessions/:id/register` และ `POST /api/sessions/:id/checkin`
- หน้า dashboard เพิ่มการ์ด QR code + ลิงก์ (copy ได้) ให้แชร์กับผู้เล่น และการ์ด "รอเช็คอิน" ให้แอดมินเช็คอินแทนคนที่ไม่มีมือถือได้
- ผู้เล่นที่แอดมินเพิ่มผ่าน dashboard ยังพร้อมลงคอร์ดได้ทันทีเหมือนเดิม (ไม่ต้องเช็คอินซ้ำ เพราะแอดมินเพิ่มตรงหน้าคอร์ด)
- Validation พื้นฐานกันชื่อมั่ว: จำกัดความยาวชื่อ 40 ตัวอักษร, จำกัดผู้เล่นต่อ session ไม่เกิน 60 คน (ยังไม่มี rate limiting ตาม IP จริง)
- ทดสอบ end-to-end ผ่าน headless browser จริง (Playwright) ครบทุก flow: ลงชื่อล่วงหน้า, walk-in เช็คอินตรง, กดชื่อตัวเองเพื่อเช็คอิน, แอดมินเช็คอินแทน, guard ปฏิเสธการจับคู่คนที่ยังไม่เช็คอิน ไม่มี console error

## v0.6.0 (2026-09-06)

- **ย้ายจาก localStorage เป็น MongoDB จริง** — เพิ่ม backend เต็มรูปแบบ: Next.js API routes + mongoose models (`sessions`, `players`, `sessionPlayers`, `matches`, `partnerHistory`) และ auth แบบ PIN ต่อ session
- เพิ่มหน้า "สร้าง session" / "ใส่ PIN เพื่อเข้าใช้งาน" — สร้าง session ใหม่ (เลือกโหมด+จำนวนคอร์ด+ตั้ง PIN) หรือกลับเข้า session เดิมด้วย PIN เดียวกันจากเครื่องไหนก็ได้
- ย้าย `home-page.tsx` ทั้งหมดจาก `useLocalStorage` มาเรียก API จริง (`src/lib/api/sessionApi.ts`) — algorithm การจับคู่ยังอยู่ฝั่ง client เหมือนเดิม เปลี่ยนแค่แหล่งข้อมูล/ปลายทาง
- Endpoint ครบสำหรับ session/player/match lifecycle: สร้าง/ดู/แก้ session, เพิ่ม/ลบ/เปลี่ยนชื่อผู้เล่น, สร้าง/เริ่ม/จบ/เปลี่ยนตัว/ย้อนกลับแมตช์, ปิดคอร์ด, รีเซ็ตสถิติ, ดู partner history
- **เปลี่ยนพฤติกรรม:** ตัด "ย้อนกลับแผนล่าสุด" (undo แผนรอบถัดไป) ออก — เปลี่ยนแผนใหม่ทำเองได้ง่ายอยู่แล้ว ไม่คุ้มที่จะสร้างระบบรองรับ; "ล้างทั้งหมด" ไม่ลบข้อมูลบนเซิร์ฟเวอร์แล้ว แค่ลืม session บนเครื่องนั้น (ข้อมูลยังอยู่ถ้ามี PIN)
- เพิ่มหน้า "วิธีใช้งาน" (`/how-to-use`) อธิบายการใช้งานทั้งหมดแบบย่อ พร้อมลิงก์จากหน้าแรกและหน้าสร้าง session
- ลบ `src/lib/useLocalStorage.ts` ที่ไม่ได้ใช้แล้วออก
- ทดสอบ end-to-end ผ่าน headless browser จริง (Playwright) ครบทุก flow หลัก ไม่มี console error

## v0.5.0 (2026-09-06)

- ปรับ logic จับคู่ให้แฟร์ขึ้น: เพิ่ม `queuedAt` (เวลาที่เข้าคิว/พักล่าสุด) เป็นตัวจัดลำดับรองจากจำนวนแมตช์ที่เล่นไปแล้ว แก้ปัญหาคนมาถึงก่อนไม่ได้เปรียบคนมาใหม่ที่จำนวนแมตช์เท่ากัน
- เพิ่มการเก็บ partner history (จำนวนครั้งที่ผู้เล่น 2 คนเคยจับคู่เป็นทีมเดียวกัน) แล้วเลือกจับทีมภายในกลุ่มที่ลงคอร์ดให้คู่ที่จับกันน้อยที่สุดก่อน แก้ปัญหาคู่ไม่ค่อยเปลี่ยนตอนเล่น 1 คอร์ด
- เพิ่มปุ่ม "ปิดคอร์ด" (พร้อม modal ยืนยัน) สำหรับปิดคอร์ดที่หมดเวลาก่อนแบบถาวร บังคับจบแมตช์ที่กำลังเล่นทันที แล้วย้ายผู้เล่นไปรวมคิวกับคอร์ดที่เหลือ
- ลบ logic `recentlyPlayedIds`/`getRoundPlayerIds` เดิมที่ไม่เคยทำงานจริง (ถูก `blockedIds` กรองออกไปก่อนถึงขั้นตอนจัดลำดับเสมอ) ออก และรวมการทำงานเข้ากับ `queuedAt` แทน
- เพิ่มการรองรับ undo/reset/clearAll ให้ครอบคลุม partner history ด้วย เพื่อไม่ให้ state ไม่ตรงกันหลังย้อนกลับ

## v0.4.2 (2026-05-22)

- ติดตั้ง `@vercel/speed-insights` เพื่อวัดประสิทธิภาพการโหลดหน้าและ Core Web Vitals
- ผูก `<SpeedInsights />` ใน root layout ให้เก็บข้อมูลประสิทธิภาพจากทุกหน้าอัตโนมัติ
- อัปเดตเวอร์ชันโปรเจกต์เป็น `v0.4.2` ตามรอบ patch release

## v0.4.1 (2026-05-22)

- ติดตั้ง `@vercel/analytics` เพื่อรองรับการเก็บสถิติการใช้งานบน Vercel
- ผูก `<Analytics />` ใน root layout เพื่อเก็บ page analytics จากทุกหน้าอัตโนมัติ
- อัปเดตเวอร์ชันโปรเจกต์เป็น `v0.4.1` ตามรอบ patch release

## v0.4.0 (2026-05-18)

- เพิ่มฟีเจอร์ "Change game" เปลี่ยนโหมดการเล่นระหว่าง session ได้ทันที
- ปรับปรุง UI/UX ให้เหมาะกับทั้ง desktop และ mobile (responsive, ปุ่ม, modal, การแจ้งเตือน)
- เพิ่ม/ปรับ logic การ undo, reset, clearAll, และการจัดการ state ให้ถูกต้องและปลอดภัย
- ลบ dead code, import ที่ไม่ได้ใช้, และ refactor โค้ดให้สะอาดขึ้น
- อัปเดต postcss เป็น 8.5.10 เพื่อปิดช่องโหว่ XSS
- ปรับปรุง useLocalStorage ให้รองรับการ clear จริง (skip persist)
- เพิ่มระบบยืนยัน (confirmation modal) ก่อนลบข้อมูลสำคัญ
- ตรวจสอบและยืนยันว่า build ผ่าน ไม่มี error
- อัปเดต README.md และ workflow การอัปเวอร์ชันให้ชัดเจน

## v0.3.0 (2026-05-16)

- แยก flow เปลี่ยนตัวผู้เล่น ออกจาก ลบผู้เล่น ชัดเจน
- เปลี่ยนตัวแล้วผู้เล่นเดิมจะไปพัก (ไม่ถูกลบออกจากรายชื่อ)
- ปรับการนับสถิติรายคนให้เกิดตอนจบแมตช์เท่านั้น
- เพิ่มการจัดการผู้เล่นผ่าน Modal (แก้ชื่อ / ลบผู้เล่น)
- เพิ่ม validation ตอนแก้ชื่อ (ห้ามว่าง, ห้ามชื่อซ้ำ, Enter เพื่อบันทึก, Esc เพื่อปิด)
- เพิ่มตัวนับแมตช์รวมที่เล่นไปแล้ว และแสดงในแถบสรุปใต้กระดานแมตช์
- ปรับปรุงเอกสาร README ให้สอดคล้องกับพฤติกรรมล่าสุดของระบบ
